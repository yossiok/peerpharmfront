import { DateExpiredDirective } from './date-expired.directive';

describe('DateExpiredDirective', () => {
  it('should create an instance', () => {
    const directive = new DateExpiredDirective();
    expect(directive).toBeTruthy();
  });
});
