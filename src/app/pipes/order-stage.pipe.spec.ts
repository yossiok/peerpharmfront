import { OrderStagePipe } from './order-stage.pipe';

describe('OrderStagePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStagePipe();
    expect(pipe).toBeTruthy();
  });
});
