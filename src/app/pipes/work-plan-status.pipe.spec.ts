import { WorkPlanStatusPipe } from './work-plan-status.pipe';

describe('WorkPlanStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkPlanStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
