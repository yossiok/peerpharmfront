import { OrderItemBatchStatusPipe } from './order-item-batch-status.pipe';

describe('OrderItemBatchStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderItemBatchStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
