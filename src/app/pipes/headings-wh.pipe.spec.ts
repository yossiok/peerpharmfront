import { HeadingsWHPipe } from './headings-wh.pipe';

describe('HeadingsWHPipe', () => {
  it('create an instance', () => {
    const pipe = new HeadingsWHPipe();
    expect(pipe).toBeTruthy();
  });
});
