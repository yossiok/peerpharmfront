import { OrderItemBatchStatusColorPipe } from './order-item-batch-status-color.pipe';

describe('OrderItemBatchStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderItemBatchStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
